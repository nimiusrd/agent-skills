# 動作保持と範囲管理

リファクタリングは外部から観測できる振る舞いを保持して内部構造を改善する変更。

- 入出力、公開インターフェース、例外、副作用の順序・回数を保持する。
- 別の機能追加、バグ修正、最適化、将来のための抽象化を混ぜない。必要なら別候補へ記録する。
- ファイル数・行数を目的にせず、一つの目的に必要な変更だけを含める。
- テストの期待値を変更後の実装に合わせて緩めない。
- 検証の不足は具体的な経路として示す。実行していない検証や未取得の指標を捏造しない。
- 次の候補に進む前に、現在の変更を独立してレビューできる状態にする。
